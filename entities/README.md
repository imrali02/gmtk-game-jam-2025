# Entities

This directory contains all game entities that can be instantiated in the game world.

## Structure

- `player/` - Contains the player character and related assets
- `collectibles/` - Contains collectible items that the player can interact with

When adding new entity types, create a new subdirectory for each type.